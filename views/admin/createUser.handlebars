
<a class="button button-primary" href="/admin/create/users">User</a>
<a class="button" href="/admin/create/farmers">Farmer</a>
<br><br>
{{#if user}}
<form action="/admin/users/{{user.id}}?_method=PUT" method="POST">
  {{else}}
  <form action="/admin/users" method="POST">
    {{/if}}
    <div class="form-group">
      <label for="name">Name</label>
      <input type="text" class="form-control" name="name" id="name" placeholder="Enter name" value="{{user.name}}">
    </div>

    <div class="form-group">
      <label for="email">Email</label>
      <input type="text" class="form-control" name="email" id="email" placeholder="Enter email" value="{{user.email}}">
    </div>

    <div class="form-group">
      <label for="tel">Tel</label>
      <input type="text" class="form-control" name="tel" id="tel" placeholder="Enter tel" value="{{user.tel}}">
    </div>

    <div class="form-group">
      <label for="address">Address</label>
      <input type="text" class="form-control" name="address" id="address" placeholder="Enter address"
        value="{{user.address}}">
    </div>

    {{!-- <div class="form-group">
      <label for="role">Role</label>
      <select class="form-control" id="role" name="role">
        {{#if user}}
        {{#ifCond user.role 'user'}}
        <option value="{{user.role}}">{{user.role}}</option>
        <option value="farmer">farmer</option>
        {{else}}
        <option value="{{user.role}}">{{user.role}}</option>
        <option value="user">user</option>
        {{/ifCond}}
        {{else}}
        <option value="" selected disabled>--please select--</option>
        <option value="user">user</option>
        <option value="farmer">farmer</option>
        {{/if}}
      </select>
    </div> --}}

    <button type="submit" class="button-primary">Submit</button>
  </form>
</div>